# Generated by confd        
filter calico_dynamic {     
{{range ls "/v1/ipam/v4/pool"}}{{$data := json (getv (printf "/v1/ipam/v4/pool/%s" .))}}
  if ( net ~ {{$data.cidr}} ) && ( net.len = 32 ) then {                                
    reject;                                                                             
  }                                                                                     
{{end}}                                                                                 
  accept;                                                                               
}  
